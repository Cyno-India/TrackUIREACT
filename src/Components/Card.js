import React from "react";

export default function Card({ item }) {
  return (
    <div
      style={{
        display: "flex",
        width: "100%",
        backgroundColor: "#09baca",
        margin: "10px",
      }}
    >
      <h1>{item.status}</h1>
      <h1>{item.status}</h1>
      <h1>{item.status}</h1>
      <h1>{item.status}</h1>
      <h1>{item.status}</h1>
      <h1>{item.status}</h1>
    </div>
  );
}
